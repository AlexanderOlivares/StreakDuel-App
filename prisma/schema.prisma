generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PotentialMatchup {
  id         String @id
  eventId    String
  gameDate   String
  gameTime   String
  name       String
  league     String
  gameStatus String
}
